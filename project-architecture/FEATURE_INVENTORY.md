# Feature inventory

Each row reflects **observed code**. Status uses: **implemented** (clear end-to-end in backend and/or wired client), **partial** (backend or UI only, or prototype-only), **unclear** (needs product confirmation).

| Feature | Purpose | Main files | Entry points | Dependencies | Status | Risks / gaps |
|---------|---------|------------|--------------|--------------|--------|--------------|
| Customer / driver / admin registration & login | Account creation and JWT session | `backend/src/auth/*`, `frontend/lib/services/auth_service.dart`, `frontend/lib/features/auth/providers/auth_provider.dart` | `POST /auth/register/customer`, `/register/driver`, `/login`; Flutter `AuthService` | MongoDB `User`; bcrypt | **Implemented** (backend); **partial** (Flutter uses API for auth; home is Stitch) | Phone login supported backend-side; token decode for role in `_AuthGate` must match API |
| Role-based app shell | Route user to prototype home by role | `frontend/lib/main.dart` (`_AuthGate`) | App launch | `authStateProvider`, Stitch assets | **Partial** | Most UX is HTML stitch, not native Flutter screens |
| User profile & preferences | CRUD profile, password, prefs, delete account | `backend/src/users/users.controller.ts`, `users.service.ts`, `frontend/lib/services/user_service.dart` | `PATCH /users/me`, `GET /users/me/preferences`, etc. | `User` schema | **Implemented** (backend); **partial** (client *needs verification* per screen) | `PATCH me/preferences` DTO typing vs nested `preferences` object |
| Saved addresses | Delivery addresses per user | `backend/src/users/addresses.controller.ts`, `users/schemas/address.schema.ts`, `frontend/lib/services/address_service.dart` | `/users/addresses` | User linkage | **Implemented** (backend); **partial** (client) | — |
| Favorites | Favorite products list | `backend/src/users/users.controller.ts` (`favoriteProductIds` on user), `users.service.ts` | `GET/POST/DELETE users/favorites/...` | `Product` refs | **Implemented** (backend); **partial** (client) | — |
| Catalog browse & search | List categories/products, related items | `backend/src/catalog/catalog.controller.ts`, `catalog.service.ts`, `frontend/lib/services/catalog_service.dart`, `features/products/` | `GET /catalog/categories`, `/products`, `/products/search`, `/products/:id` | Mongo catalog schemas | **Implemented** | Public read endpoints — ensure no sensitive data in product docs |
| Catalog admin CRUD | Admin maintains categories/products | `catalog.controller.ts` (guarded routes) | `POST/PATCH/DELETE` under `/catalog/...` | Admin JWT | **Implemented** | — |
| Orders | Customer orders + reorder; admin status | `backend/src/orders/orders.controller.ts`, `orders.service.ts`, `frontend/lib/services/orders_service.dart` | `/orders` | User, order schema | **Implemented** (backend); **partial** (client) | Admin-only status update |
| Deliveries lifecycle | Create delivery, driver accept/start/complete, cancel, tip, rate | `backend/src/deliveries/*`, `delivery-matching.service.ts`, `frontend/lib/features/delivery/`, `services` | `/deliveries` | `Delivery` schema, `DeliveryGateway` | **Implemented** (backend + WS); **partial** (client) | Some routes lack `@Roles`; idor risk — see security doc |
| Real-time delivery updates | Push status/location to subscribers | `backend/src/websocket/delivery.gateway.ts`, consumers in `deliveries.service.ts`, `drivers.service.ts` | Socket.IO namespace `/delivery` | JWT in handshake | **Implemented** | CORS `*`; room subscription not verified against ownership |
| Driver profile & ops | Availability, motorcycle link, documents via driver, verification admin | `backend/src/drivers/drivers.controller.ts`, `drivers.service.ts`, `frontend/lib/features/driver/` | `/drivers/...` | `Driver` schema, `User` | **Implemented** | Many driver routes have no `@Roles`; **`GET /drivers/leaderboard` likely broken** (declared after `:id` — see `API_MAP.md`) |
| Motorcycles registry | CRUD motorcycles | `backend/src/motorcycles/motorcycles.controller.ts`, `frontend/lib/features/motorcycles/` | `/motorcycles` | `Motorcycle` schema | **Implemented** | `GET` list/one: any authenticated user |
| Documents verification | Upload files, admin review | `backend/src/documents/documents.controller.ts`, `documents.service.ts`, `frontend/lib/services/documents_service.dart` | `/documents/upload`, etc. | Disk + `Document` schema | **Implemented** | `GET /documents/:id` without `@Roles` |
| Admin console APIs | Dashboard, pending lists, verify/reject, reports | `backend/src/admin/admin.controller.ts`, `admin.service.ts`, `frontend/lib/features/admin/providers/admin_provider.dart` | `/admin/*` | Various services | **Implemented** (backend); **partial** (Stitch admin UI) | — |
| Surge pricing rules | Admin rules + preview; current multiplier | `backend/src/surge/surge.controller.ts`, `surge-pricing.controller.ts` | `/surge-rules`, `/surge-pricing/current` | Surge schema | **Implemented** | — |
| Promo codes | Validate and apply discounts | `backend/src/promo-codes/promo-codes.controller.ts` | `/promo-codes/validate`, `/apply` | Service implementation | **Implemented** | Customer + admin only (`@Roles` on controller) |
| Recommendations | Personalized recs + FBT | `backend/src/recommendations/recommendations.controller.ts`, `recommendations.service.ts` | `GET /recommendations`, `GET /products/:id/fbt` | Orders/catalog *inferred* | **Implemented** | No `@Roles` on methods — guard passes if no roles metadata |
| Notification preferences | Category-based prefs | `backend/src/notifications/notifications.controller.ts`, `notifications.service.ts` | `/notification-preferences` | Notification preference schema | **Implemented** | DTO uses `categories` — align with user nested preferences *needs verification* |
| Notification inbox | In-app notifications list/read/delete | `backend/src/notifications/notification-inbox.controller.ts` | `/notifications` | `notification.schema.ts` | **Implemented** | No `@Roles` — any authenticated role |
| Support tickets & feedback | User tickets; admin ticket list/status | `backend/src/support/support.controller.ts`, `support.service.ts`, `frontend/lib/services/support_service.dart`, `settings/providers/support_provider.dart` | `/support/tickets`, `/admin/support/...`, `/feedback` | Ticket/feedback schemas | **Implemented** | `GET /faqs` still requires JWT (class-level guard) |
| FAQ admin | Create FAQ entries | `support.controller.ts` | `POST /faqs` | FAQ schema | **Implemented** | — |
| Firebase FCM | Register device token; send test push | `backend/src/firebase/firebase.controller.ts`, `firebase.service.ts` | `/firebase/register-token`, `/firebase/send-push` | Firebase Admin | **Partial** | `send-push` not admin-restricted |
| Health & system info | Liveness / DB ping; version string | `backend/src/health/health.controller.ts`, `system.controller.ts` | `GET /health`, `GET /system/version` | Terminus | **Implemented** | Exposes infra signal — acceptable if not public internet or rate-limited |
| Maps & routing (client) | Geocoding, routes for UI | `frontend/lib/services/osrm_service.dart`, `nominatim_service.dart`, map widgets | N/A (external HTTP) | Public OSM ecosystem | **Partial** | Third-party uptime/terms |
| Stitch UI gallery | HTML design prototypes | `frontend/stitch/*/code.html`, `frontend/lib/stitch/stitch_viewer.dart` | Named routes in `main.dart` | `webview_flutter` | **Partial** | Not substitute for production accessibility/perf testing |

## Legend

- **Entry points**: primary API route or Flutter route/widget.
- **Unclear** was avoided above where code exists; remaining **partial** items mean native UI or full E2E wiring is not confirmed from static analysis alone.
